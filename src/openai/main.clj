(ns openai.main
  (:require
   [openai.audio]
   [openai.completions]
   [openai.files]
   [openai.responses]))

(defn -main [& _args]
  (println "Require the namespaces you want to use from the REPL."))
